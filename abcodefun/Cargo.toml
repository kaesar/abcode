[package]
name = "abcodefun"
version = "0.7.0"
edition = "2021"
description = "ABCode Functions - Serverless ABCode execution runtime"

[dependencies]
feather = { version = "0.8", features = ["jwt"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
abcodelib = { path = "../abcodelib" }
